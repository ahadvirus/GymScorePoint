define(function (require) {
    'use strict';

    const React = require('react');

    return function () {

        return (
            React.createElement('h1', { }, 'Hello world!')
        );
    }
});